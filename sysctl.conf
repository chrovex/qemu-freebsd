# $FreeBSD$
#
#  This file is read when going to multi-user and its contents piped thru
#  ``sysctl'' to adjust kernel values.  ``man 5 sysctl.conf'' for details.
#

# Uncomment this to prevent users from seeing information about processes that
# are being run under another UID.
#security.bsd.see_other_uids=0

# Kernel with the EVDEV_SUPPORT option enabled (default starting from FreeBSD
# 12.1) is recommended to enable evdev mode in�pointer device drivers.
# This will give improvements like better tilt wheel support for mice and
# centralized gesture via xf86-input-synaptics or libinput for touchpads.
kern.evdev.rcpt_mask=6
